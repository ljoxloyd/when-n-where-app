import React from 'react'

interface AboutSectionProps {}

const AboutSection = (props: AboutSectionProps) => {
  return <section className="shadow-md bg-white rounded-md h-full"></section>
}

export default AboutSection
